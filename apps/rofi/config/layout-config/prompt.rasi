@import "../font.rasi"
@import "../colors.rasi"

prompt {
    background-color:               @ON;
    text-color:                     @NBG;
    border-radius:                  10px;
    padding:                        10px;
    font:                           @bold;
    horizontal-align:               0.5;
    vertical-align:                 0.5;
}
